{
  "name": "productivity-app",
  "version": "1.0.0",
  "description": "Productivity app with PHP and vanilla JavaScript",
  "scripts": {
    "format": "prettier --write .",
    "test:backend": "php vendor/bin/phpunit --configuration phpunit.xml",
    "test:backend:docker": "docker compose run --rm composer install && docker compose run --rm web php /var/www/vendor/bin/phpunit --configuration /var/www/phpunit.xml"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.25.0",
    "prettier": "^3.8.3"
  }
}
