{
  "name": "productivity-app",
  "version": "1.0.0",
  "description": "Productivity app with PHP and TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.25.0",
    "prettier": "^3.8.3",
    "typescript": "latest"
  }
}
